# Backend API base URL (Fastify webhook + admin API)
# In dev: leave empty and use Vite's proxy (vite.config.ts) — same-origin avoids CORS.
# In prod: set to the deployed backend host if it's served from a different origin.
VITE_API_BASE_URL=
